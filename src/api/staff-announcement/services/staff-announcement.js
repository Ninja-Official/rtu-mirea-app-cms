'use strict';

/**
 * staff-announcement service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::staff-announcement.staff-announcement');
